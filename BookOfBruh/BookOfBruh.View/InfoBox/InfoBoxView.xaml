<Window x:Class="BookOfBruh.View.InfoBox.InfoBoxView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BookOfBruh.View.InfoBox"
        xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
        mc:Ignorable="d"
        Title="InfoBoxView" Height="450" Width="800" 
        WindowStyle="None"
        d:DataContext="{d:DesignInstance local:InfoBoxViewModel}">

    <b:Interaction.Triggers>
        <b:EventTrigger EventName="Deactivated">
            <b:InvokeCommandAction Command="{Binding LostFocusCommand}"></b:InvokeCommandAction>
        </b:EventTrigger>
    </b:Interaction.Triggers>

    <Grid>
       <Label
            Style="{StaticResource CenteredBlackLabel}"
            Content="{Binding Message}"/>
    </Grid>
</Window>
