<UserControl
    x:Class="BookOfBruh.View.Control.ControlsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="3*" />
            <RowDefinition Height="2*" />
            <RowDefinition Height="2*" />
            <RowDefinition Height="2*" />
            <RowDefinition Height="2*" />
            <RowDefinition Height="5*" />
        </Grid.RowDefinitions>

        <Label
            Grid.Row="1"
            Grid.Column="0"
            Style="{StaticResource CenteredGrayLabel}"
            Content="BruhCoins"/>

        <Label
            Grid.Row="2"
            Grid.Column="0"
            Style="{StaticResource CenteredBorderedLabel}"
            Content="{Binding Guthaben, UpdateSourceTrigger=PropertyChanged}"/>

        <Label
            Grid.Row="3"
            Grid.Column="0"
            Style="{StaticResource CenteredGrayLabel}"
            Content="Einsatz"/>

        <TextBox
            Grid.Row="4"
            Grid.Column="0"
            Margin="10"
            IsReadOnly="True" />

        <Button
            Grid.Row="5"
            Grid.Column="0"
            Style="{StaticResource CenteredButton}"
            Command="{Binding SpinClickCommand}"
            Content="Spin"/>
    </Grid>
</UserControl>
